{
  "name": "asink",
  "version": "1.0.0",
  "description": "Simple project to demo Asyc Await",
  "main": "index.js",
  "scripts": {
    "test": "npm test",
    "build": "rimraf dist/ && babel ./ --out-dir dist/ --ignore ./node_modules,./.babelrc,./package.json,./npm-debug.log --copy-files",
    "start": "npm run build && node dist/index.js"
  },
  "keywords": [
    "async",
    "await"
  ],
  "author": "Ganesh Chaitanya Kale",
  "license": "ISC",
  "devDependencies": {
    "@babel/core": "^7.5.5",
    "@babel/preset-env": "^7.5.5",
    "babel-cli": "^6.26.0"
  },
  "dependencies": {
    "axios": "^0.19.0",
    "babel-preset-es2015": "^6.24.1",
    "node-fetch": "^2.6.0",
    "rimraf": "^2.6.3"
  }
}
